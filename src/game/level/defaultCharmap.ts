import { blueCoin, standardCoin, standardWall } from "../tiles/tileTemplates";

// Default charmap (works well with spread operator)
const defaultCharMap = {
    "A": standardWall,
    "B": blueCoin,
    "C": standardCoin
}

export default defaultCharMap;