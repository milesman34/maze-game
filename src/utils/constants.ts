// Total size of the canvas in pixels
const canvasSize = 640;

// Default size of an object/tile
const tileSize = 32;

// Default number of tiles per screen
const numTiles = 20;

export {canvasSize, numTiles, tileSize}