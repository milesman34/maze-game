// Enum that represents the game state
const GameState = {
    Title: 0,
    Game: 1
}

// Direction enum
const Direction = {
    Left: 0,
    Right: 1,
    Up: 2,
    Down: 3
}

export {
    Direction,
    GameState
}