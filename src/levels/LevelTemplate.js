// This object represents a template for a level to be created with the Level.loadFromTemplate function
const LevelTemplate = (stringArray, charMap, params) => ({
    stringArray,
    charMap,
    params
});

export default LevelTemplate;